-- database: :memory:
CREATE TABLE Bonus (
    WORKER_REF_ID INT,
    BONUS_DATE TIMESTAMPS,
    BONUS_AMOUNT,
    FOREIGN KEY (WORKER_REF_ID) REFERENCES Worker(WORKER_ID)
    ON DELETE CASCADE
    ON UPDATE CASCADE
);

-- DROP TABLE Bonus;