-- database: :memory:
CREATE TABLE Worker(
    WORKER_ID INT PRIMARY KEY,
    FIRST_NAME VARCHAR(20),
    LAST_NAME VARCHAR(20),
    SALARY DOUBLE,
    JOINING_DATE TIMESTAMPS ,
    DEPARTMENT VARCHAR(10)
 );